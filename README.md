# 🗂️ tmp-files — Temporary File Uploader (Go + Gin)

A simple, lightweight **temporary file hosting service** written in **Go (Golang)** using the **Gin** web framework.  
Upload files, get a temporary URL, and let them auto-delete after 60 minutes — **no database required**.

---

## 🚀 Features

- 🧾 Upload any file up to **50 MB**
- 🕒 Files auto-deleted after **60 minutes**
- 🖼️ Image preview (PNG, JPG, GIF)
- ⚙️ No database — just filesystem-based
- 🧹 Background cleaner runs every 10 minutes
- 🐳 Fully containerized with **Docker Compose**

---